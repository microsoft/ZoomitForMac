interface:
  display_name: "ZoomitForMac"
  short_description: "Repo-specific patterns and workflows for ZoomitForMac"
  default_prompt: "Use the ZoomitForMac repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true